[project]
name = "partiii-compositional-agents-process"
version = "0.1.0"
description = "Part III compositional-agent synthesis pipeline."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["partiii_compositional_agents*"]
